# Backend — compiled output, not source
backend/target/
*.class

# Frontend — installed packages and build output, both regenerable
frontend/node_modules/
frontend/.next/
frontend/.env*.local

# General — machine-specific or secret files
.env
.DS_Store
*.log
